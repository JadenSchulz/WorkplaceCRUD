<UserControl x:Class="Employee_WPF_MVVM_CRUD.Views.EmployeeViews.EmployeesMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Employee_WPF_MVVM_CRUD.Views.EmployeeViews"
             mc:Ignorable="d" Background="Snow"
             d:DesignHeight="720" d:DesignWidth="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>

        <DockPanel LastChildFill="false" Grid.Column="0">
            
            <StackPanel DockPanel.Dock="Top">
                <TextBlock HorizontalAlignment="Center" FontSize="18" FontWeight="Bold">Employees</TextBlock>
                <Button>Salary History</Button>
                <Button>Title History</Button>
                <Button>Mgmt History</Button>
                <Button>Dept History</Button>
            </StackPanel>
            <StackPanel DockPanel.Dock="Bottom">

                <Button>Edit Departments</Button>
            </StackPanel>
        </DockPanel>


        <ContentControl Grid.Column="1" Content="{Binding CurrentView}"/>


    </Grid>
</UserControl>
